// The app's schema is multi-branch; this website only ever operates on the
// single seeded branch (supabase/seed.sql in anchor-drops-system).
export const DEFAULT_BRANCH_ID = '00000000-0000-0000-0000-000000000001';
